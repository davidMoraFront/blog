import { AppPage } from './app.po';
import { browser, logging } from 'protractor';

describe(`<${AppPage.HtmlTagComponent}>`, () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });
});
